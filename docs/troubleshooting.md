# Troubleshooting

A running log of real problems, their causes and fixes. Add an entry whenever something breaks — this is evidence of understanding, not clutter.

Format: **Symptom** → **Cause** → **Fix** → **How to confirm**.

## Phase 1: local platform

### Encountered on a real machine (Windows 11, WSL2 Ubuntu 24.04, 2026-09-20/21)

| Symptom | Cause | Fix | Confirm |
|---------|-------|-----|---------|
| `apt update`: `Conflicting values set for option Signed-By` after following an older version of the Docker install steps | Docker was already installed with `docker.list` + `docker.gpg`; a second source file (`docker.sources` + `docker.asc`) declared the same repository with a different key file | `sudo rm /etc/apt/sources.list.d/docker.sources`. Check for existing Docker first (`docker --version`). Never keep two source files for one repository | `sudo apt update` succeeds (an unrelated `NO_PUBKEY` warning for a third-party repository may remain; it is harmless here) |
| `make cluster-up` times out; `k3d-polaris-agent-0` never registers with the API server, with k3s v1.35.8 | The host uses **cgroup v1** (`stat -fc %T /sys/fs/cgroup` prints `tmpfs`; `docker info` shows `Cgroup Version: 1`; WSL2 kernel 5.15.167.4). Kubernetes 1.35+ refuses to start the kubelet on cgroup v1 by default | Pin k3s to 1.34: `image: rancher/k3s:v1.34.10-k3s1` in `cluster.yaml` and `KUBECTL_VERSION=v1.34.10` in `versions.env`; `make cluster-reset`. Long-term: move WSL2 to cgroup v2 (see ADR-16). `make doctor` now detects the combination | `kubectl --context k3d-polaris get nodes` shows 3 nodes Ready on `v1.34.10+k3s1` |
| `Bind for 0.0.0.0:8080 failed: port is already allocated` | Another local cluster (a `kind` container) already published 8080/8443 | Find the holder: `docker ps --format '{{.Names}}  {{.Ports}}' \| grep 8080`. Change the host ports in `deploy/k3d/cluster.yaml` (now 8088/8448); the smoke test reads the port from that file. `make doctor` now checks the ports before the cluster exists | `make cluster-up` succeeds; `docker ps` shows the load balancer on the new ports |
| `make tools-install`: `Connection reset by peer` while downloading kubectl | A transient network reset | Re-run. Downloads now use `curl --retry 5 --retry-all-errors` | The tool prints its version after install |
| `make smoke` step 3: rollout times out; pods `ImagePullBackOff` with `dial tcp [::1]:5000: connect: connection refused` for `k3d-registry.localhost:5000/...` | The registry container is named exactly as in `cluster.yaml` (`registry.localhost`); there is **no** `k3d-` prefix. The name `k3d-registry.localhost` has no containerd mirror, so containerd tried plain HTTPS on localhost | Use `registry.localhost:5000/<repo>:<tag>` inside the cluster and `localhost:5000/<repo>:<tag>` from the host. `make test` now checks the manifest against the registry name in `cluster.yaml` | `docker exec k3d-polaris-server-0 cat /etc/rancher/k3s/registries.yaml` lists the mirror `registry.localhost:5000`; `make smoke` prints `SMOKE TEST PASSED` |
| `make cluster-up` (fresh cluster): nodes Ready, but `Traefik deployment did not appear ... within 180s`; `make smoke` step 5 then gets no response on the ingress port | On a new cluster the nodes pull their own system images from Docker Hub. Events showed `Failed to pull image "rancher/klipper-helm:...": ... failed to fetch anonymous token: Get "https://auth.docker.io/token...": context canceled` (a cancelled token request, not a `toomanyrequests` rate limit). Failed pulls back off before retrying, so Traefik's installer started late. `helm-install-traefik` also failed once with `Required CRDs are missing` (it races the CRD chart) and succeeded on its automatic retry | Wait. Traefik appeared about 5.5 minutes after cluster creation without any action. `cluster.sh` now waits up to 600 s (`POLARIS_TRAEFIK_WAIT=900 make cluster-up` for longer) and prints pods and events if it still times out. If it never recovers: `kubectl -n kube-system get pods,jobs` and the events | `kubectl -n kube-system get deploy traefik` shows `1/1`; `helm-install-traefik` pod is `Completed` (one restart is normal); `make smoke` passes |
| `wsl --terminate` typed inside Ubuntu does nothing useful | `wsl` is a Windows command | Run it in Windows PowerShell | `wsl -l -v` (PowerShell) shows the distro as Stopped |

Diagnostic habit that found the registry-name problem: the smoke test deletes its namespace on failure, so deploy the manifest by hand (`kubectl apply -f deploy/k8s-smoke/whoami.yaml`) and read `kubectl -n polaris-smoke describe pods` (the Events section). The smoke test now prints pod status and events itself when the rollout fails.

### Expected failure modes (not yet reproduced)

These come from the design. They are marked *expected* until reproduced on a real machine; replace the marker with the date and the actual output when that happens.

| Symptom | Likely cause | Fix | Confirm |
|---------|--------------|-----|---------|
| `make doctor`: "systemd is not PID 1" *(expected)* | systemd not enabled for the WSL distro | Add `[boot]` / `systemd=true` to `/etc/wsl.conf`, then `wsl --shutdown` in PowerShell | `ps -p 1 -o comm=` prints `systemd` |
| `permission denied while trying to connect to the Docker daemon socket` *(expected)* | User not in the `docker` group, or WSL not restarted after `usermod` | `sudo usermod -aG docker "$USER"`, then run `wsl --terminate <DistroName>` in Windows PowerShell and reopen Ubuntu | `docker ps` works without sudo |
| `Cannot connect to the Docker daemon` *(expected)* | Docker service not running | `sudo systemctl enable --now docker` | `systemctl status docker --no-pager` |
| `/usr/bin/env: 'bash\r': No such file or directory` *(expected)* | Scripts saved with Windows (CRLF) line endings | `sed -i 's/\r$//' <file>`; keep `.gitattributes` in the repo | `make test` (CRLF check) passes |
| `make: *** missing separator` *(expected)* | Makefile indented with spaces instead of tabs | Recipes must start with a real tab (see `.editorconfig`) | `make help` prints the target list |
| `make doctor`: RAM below the expectation *(expected)* | WSL2 default memory cap (50 % of Windows RAM) | Set `memory=` in `%UserProfile%\.wslconfig`, `wsl --shutdown` | `free -h` shows the new total |
| `k3d cluster create` times out *(expected)* | Not enough RAM/CPU, or slow first-time image pulls | Raise `.wslconfig` limits; retry `make cluster-reset` | `make cluster-status` shows all nodes Ready |
| `Could not pull rancher/k3s:...` *(expected)* | Tag not available or Docker Hub unreachable/rate-limited | Pick another tag of the same Kubernetes minor (`v1.34.x-k3sN`) in `deploy/k3d/cluster.yaml`; keep kubectl within one minor | `docker pull <image>` succeeds |
| Port 5000 already in use *(expected)* | Another local registry uses it | `ss -ltnp 'sport = :5000'`; stop it or change `hostPort` in `cluster.yaml` (and the `PUSH_REF` in `smoke-test.sh`) | `make cluster-up` succeeds |
| `make smoke`: no response from ingress *(expected)* | Traefik not ready yet, or port mapping changed | `kubectl -n kube-system get pods`; `kubectl -n polaris-smoke get ingress,pods` | `curl -H 'Host: whoami.localhost' http://localhost:8088/` returns text (use the port from `cluster.yaml`) |
| `make smoke`: `Cannot pull traefik/whoami:...` *(expected)* | Tag unavailable or Docker Hub rate limit | `SMOKE_IMAGE=traefik/whoami:latest make smoke` | Smoke test passes |
| Everything is slow, file watching misbehaves *(expected)* | Repository stored under `/mnt/c/...` | Move it to `~/polaris/...` on the Linux filesystem | `make doctor` shows the repo on the Linux filesystem |

## Phase 2: AI service

Marked *expected* until reproduced on the target machine. The last three were reproduced in the sandbox while testing (2026-09-21) and are marked *seen*.

| Symptom | Likely cause | Fix | Confirm |
|---------|--------------|-----|---------|
| `make app-install`: `The virtual environment was not created successfully because ensurepip is not available` *(expected)* | Ubuntu splits `venv` into a separate package | `sudo apt-get install -y python3-venv`; remove the half-made `app/ai_service/.venv` and retry | `make doctor` shows "python3 can create virtualenvs" |
| `pip install -e`: `requires a different Python: 3.10.x not in '>=3.12'` *(expected)* | `python3` on PATH is older than 3.12 | Use Ubuntu 24.04 (Python 3.12), or create the venv with `python3.12 -m venv app/ai_service/.venv` by hand | `app/ai_service/.venv/bin/python --version` prints 3.12 or newer |
| `make app-test`: `No module named ai_service` *(expected)* | The package was not installed into the venv | `make app-install` (the last step is `pip install -e`) | `.venv/bin/python -c "import ai_service"` |
| `make app-run`: `Address already in use` on port 8000 *(expected)* | Another process uses 8000 | `ss -ltnp 'sport = :8000'`; stop it, or run uvicorn by hand with `--port 8001` | `curl http://127.0.0.1:8000/docs` answers |
| Service refuses to start: `POLARIS_OPENAI_MODEL must be set when POLARIS_BACKEND=openai_compat` *(seen)* | The real backend was selected without a model name | Set `POLARIS_OPENAI_MODEL` (the model is chosen in Phase 11), or unset `POLARIS_BACKEND` to use the mock | The service starts |
| `POST /v1/chat` returns 502 `backend_error` with `reason=model backend unreachable` in the log *(seen)* | `POLARIS_BACKEND=openai_compat` but nothing listens on `POLARIS_OPENAI_BASE_URL` | Start the model server (Phase 11) or use the mock. Failure injection with `POLARIS_MOCK_FAILURE_RATE` returns the same 502 on purpose | Log shows `chat completed` |
| `POST /v1/chat` returns 504 `backend_timeout` *(seen)* | Backend slower than `POLARIS_BACKEND_TIMEOUT_S` (or `POLARIS_MOCK_LATENCY_MS` set higher than the budget) | Raise the timeout or lower the latency | The request returns 200 |

## Phase 3: container image, probes, logs, scanner

Rows marked *(expected)* come from the design and from how these tools usually fail; they were not reproduced. Rows marked *(observed 2026-09-21)* were seen on the target machine. The first build, check, push, scan and SBOM on that machine all passed without a failure, so most rows below are still expected.

| Symptom | Likely cause | Fix | Confirm |
|---------|--------------|-----|---------|
| `make image-build`: `failed to fetch anonymous token` or `toomanyrequests` while pulling `python:3.12-slim-trixie` *(expected)* | Docker Hub is slow, cancelled the request, or rate-limits anonymous pulls (the same family of problem as the Traefik pull in Phase 1) | Retry; `docker login` raises the limit; pull once with `docker pull python:3.12-slim-trixie` and build again | `docker images python` lists the tag |
| `make image-build`: `manifest unknown` for `python:3.12-slim-trixie@sha256:...` *(expected)* | `PYTHON_BASE_DIGEST` in `versions.env` was mistyped or copied for another tag | Empty the value, or run `make image-pin` and paste the printed line | `make image-info` prints the base image; the build finds it |
| `make image-build`: `No matching distribution found ... --only-binary` *(expected)* | A pinned dependency has no binary wheel for this platform; the image deliberately refuses to compile | Pin a version that ships a wheel (check `pip download --only-binary=:all:` in a scratch venv), then `make app-check` | The build passes the `pip install` step |
| Tag ends in `-dirty` *(expected)* | The git working tree has uncommitted changes | Commit, then `make image-build` again. A `-dirty` image is for local tests only, never for `make image-push` in a real workflow | `make image-info` shows a tag without `-dirty` |
| `make image-check`: `does not run as a non-root user` *(expected)* | The `USER` line was changed or removed | Restore `USER 10001:10001`; `make test` also catches it | `docker run --rm --entrypoint id <image> -u` prints `10001` |
| `make image-check`: `/healthz did not answer ok` *(expected)* | The container exited at start (bad configuration, a missing file) | `docker logs polaris-ai-service-check` (the check container is removed at the end; run `make image-run` to keep it in the foreground). A misconfiguration shows as a JSON line with an `exception` field | `make image-check` shows all checks passing |
| `make image-check`: `some log lines are not JSON` *(expected)* | Something writes to stdout outside the logging setup (a `print`, a library warning) | Find it in `docker logs`; route it through the `logging` module | Every line parses with `jq` |
| `make image-check`: SIGTERM exit code 137 *(expected; on 2026-09-21 the exit code was 0 and `Application shutdown complete` was logged)* | The process did not stop within 10 s and was killed. Signals sent to PID 1 are not handled the same as for other processes | Run with `docker run --init` (tini) or check for blocking work in the lifespan shutdown | Exit code 0 or 143, and `Application shutdown complete` in the logs |
| `make image-push`: `The registry at localhost:5000 does not answer` *(expected)* | The k3d cluster (and its registry) is not running | `make cluster-up`; `docker ps --filter name=registry` | `curl -s localhost:5000/v2/_catalog` answers |
| `make image-scan`: `manifest unknown` for the Trivy image *(expected; the tag `ghcr.io/aquasecurity/trivy:0.74.0` did exist on 2026-09-21)* | The tag was mistyped or the registry name differs | `docker pull` the tag by hand; if the tag really does not exist, edit `TRIVY_IMAGE` (and if needed `TRIVY_VERSION`) in `versions.env` (an exported variable does not override that file), for example to `docker.io/aquasec/trivy`, after checking the project's installation page for the current image name and tag format | The scan starts and prints the operating system |
| `make image-scan`: the first run sits for minutes and prints a wall of download progress lines *(observed 2026-09-21)* | Trivy downloads its vulnerability database on the first run (about four minutes here, from `mirror.gcr.io/aquasec/trivy-db:2`) and prints the progress bar into a non-terminal log. Later runs use `artifacts/trivy-cache`. `--no-progress` was added afterwards to silence the bar; that flag has not been confirmed by a rerun yet | Wait; do not interrupt. Second run is fast | The report file appears in `artifacts/` |
| `make image-scan`: `TOOMANYREQUESTS` while downloading the vulnerability database *(expected)* | The default database registry rate-limits anonymous downloads | Retry later, or point Trivy at a mirror: `TRIVY_DB_REPOSITORY=public.ecr.aws/aquasecurity/trivy-db make image-scan` (any exported `TRIVY_*` variable is passed on; confirm the mirror name in the Trivy documentation first) | The report file appears in `artifacts/` |
| `make image-scan`: `permission denied` on `artifacts/trivy-cache` *(expected)* | The directory was created by an earlier run as root | `sudo rm -rf artifacts/trivy-cache`, run again (the script runs the scanner as your user) | The scan writes its report |
| `make image-scan` fails with `N HIGH/CRITICAL finding(s) have a fix available` *(expected)* | The base image or a dependency has a known vulnerability with a fix | Rebuild on a newer base (`make image-pin` for the new digest), or bump the dependency in `requirements.txt` and run `make app-check`; scan again. Record anything you accept in `docs/security/image-scan.md` | The scan prints "no HIGH/CRITICAL finding with an available fix" |

## Phase 4: Kubernetes deployment

Most rows are still *expected*: written from the manifests and from how `kubectl`/Kubernetes usually fails, not reproduced. The full deploy sequence (`make image-build`, `make image-push`, `make deploy-apply`, `make deploy-status`, `make deploy-smoke`, `make deploy-logs`) was run end to end on the target machine on 2026-09-22 and none of these symptoms occurred — image tag `0.3.0-c446b6b88521`, 2/2 pods reached `Ready`, the NetworkPolicy step did not disrupt them, and the smoke test passed all three checks on the first try. The NetworkPolicy row below is updated with that result; the rest remain unreproduced and are replaced with the date and real output the first time each one happens.

| Symptom | Likely cause | Fix | Confirm |
|---------|--------------|-----|---------|
| `make deploy-apply`: `Tag <version>-<sha> is not in the local registry` *(expected)* | The image for the current commit was never built or pushed | `make image-build && make image-push` | `curl -s localhost:5000/v2/polaris/ai-service/tags/list` lists the tag |
| `make deploy-apply`: rollout times out; pods `Pending` *(expected)* | Not enough CPU/memory on the 6-CPU/10.7GB machine across 3 k3d nodes, or the namespace's Pod Security Admission rejected the pod | `kubectl -n polaris-dev describe pod <name>` — a PSA rejection appears as an admission error naming the violated rule, not as a normal Pending reason | `kubectl -n polaris-dev get pods` shows `Running` |
| `make deploy-apply`: rollout times out; pods `ImagePullBackOff` *(expected)* | The Deployment's image reference does not use the in-cluster registry name (`registry.localhost:5000`), or the tag was pushed to a different name | `kubectl -n polaris-dev describe pod <name>` shows the attempted reference; compare with `scripts/deploy/app.sh info` | `kubectl -n polaris-dev get pods` shows `Running` |
| `make deploy-apply`: pods `Running` but never `Ready`; describe shows `Readiness probe failed: HTTP probe failed with statuscode: 503` *(expected)* | `POLARIS_MOCK_READY` in the ConfigMap is `"false"`, or the container has not finished starting within the `startupProbe` budget | `kubectl -n polaris-dev logs -l app.kubernetes.io/name=ai-service`; check `POLARIS_MOCK_READY` in `deploy/k8s/ai-service/configmap.yaml` | `/readyz` answers `{"status":"ready"}` |
| `make deploy-apply`: after the NetworkPolicy step, pods that were Ready become not Ready, or `make deploy-smoke` starts failing right after "applying NetworkPolicy" *(did not happen on the target machine, 2026-09-22 — see ADR-22)* | The `NetworkPolicy` blocks more than intended: on some CNIs, kubelet's own probe traffic (from the node, not from a pod) is not exempted from an "Ingress" policy. On this machine's k3s/k3d setup (kube-router's embedded netpol controller), `scripts/deploy/app.sh apply` reported both pods still `Ready` 10 seconds after the policy was applied, and `make deploy-smoke` passed straight after. This is one data point on one cluster config, not a guarantee for every CNI | If it does happen on a different setup: remove just the policy: `kubectl -n polaris-dev delete networkpolicy ai-service-default-deny ai-service-allow-ingress`. The Deployment/Service/Ingress do not depend on it. Record what happened here and in `docs/adr/README.md` ADR-22 | Pods return to `Ready`; `make deploy-smoke` passes |
| `make deploy-smoke`: `No response from .../healthz` *(expected)* | The Ingress host header does not match, or the k3d load balancer port differs from `deploy/k3d/cluster.yaml` | `curl -v -H 'Host: ai.localhost' http://localhost:8088/healthz`; `kubectl -n polaris-dev get ingress` | `make deploy-smoke` reaches `/healthz` |
| `kubectl apply` rejects a manifest with a Pod Security "restricted" violation naming a specific field *(expected)* | A hand-edit removed one of the hardening fields (`runAsNonRoot`, `seccompProfile`, `capabilities.drop`, ...) | Compare against `deploy/k8s/ai-service/deployment.yaml`; `tests/bootstrap/test_static.sh` checks for the same fields and fails first if they are missing | `make test` passes; `kubectl apply` succeeds |
| `make deploy-delete` then `make deploy-apply` recreates pods but the old `-dirty` image is still running *(expected)* | `imagePullPolicy: IfNotPresent` reused a cached image because the tag did not actually change (working tree was dirty both times, so the tag was identical) | Commit first (Phase 3 guidance: a `-dirty` tag is for local tests only); rebuild | `kubectl -n polaris-dev get pods -o jsonpath='{.items[0].spec.containers[0].image}'` matches `scripts/deploy/app.sh info` |

## Phase 5: Helm chart (`helm/ai-platform`)

The full sequence (`make helm-lint`, `make deploy-delete`, `make helm-apply-dev/-staging/-prod`, `make helm-status-*`, `make helm-smoke-*`, `make helm-logs-dev`) was run end to end on the target machine on 2026-09-22 with the real Helm 4.3.0 binary. `helm lint` passed for all three values files on the first try. `staging` and `prod` installed cleanly on the first `helm upgrade --install` (2/2 and 3/3 pods `Ready`, NetworkPolicy held, smoke passed). `dev` hit two real, previously undocumented problems — both rows below — each fixed once, then confirmed working (`dev` reached `REVISION: 2`, 2/2 pods, NetworkPolicy held, smoke passed). Rows are updated with these results; the rest remain *(expected)*, replaced with the date and real output the first time each one happens.

| Symptom | Likely cause | Fix | Confirm |
|---------|--------------|-----|---------|
| `make helm-apply-dev`: `Error: unable to continue with install: Namespace "polaris-dev" ... invalid ownership metadata; missing key "app.kubernetes.io/managed-by": must be set to "Helm"` *(observed 2026-09-22)* | The chart's own `templates/namespace.yaml` manages the Namespace object, not just the workload inside it. `polaris-dev` already existed — created by Phase 4's raw `kubectl apply`, never by Helm — so it carries none of the `app.kubernetes.io/managed-by`/`meta.helm.sh/release-name`/`meta.helm.sh/release-namespace` metadata Helm requires before it will treat an existing object as belonging to a release. `staging` and `prod` never hit this because those namespaces did not exist before Phase 5, so Helm created and owned them from the start. `make deploy-delete` (ADR-23's migration step) only ever removed the `ai-service` workload resources, never the namespace itself, so it does not address this | Adopt the existing namespace into the release by hand, once, before the first `make helm-apply-dev`: `kubectl --context k3d-polaris label namespace polaris-dev app.kubernetes.io/managed-by=Helm --overwrite`, then `kubectl --context k3d-polaris annotate namespace polaris-dev meta.helm.sh/release-name=ai-platform-dev --overwrite` and `meta.helm.sh/release-namespace=polaris-dev --overwrite`. This is Helm's own documented pattern for adopting a pre-existing resource, applied to exactly one object (the Namespace), not the whole migration | `make helm-apply-dev` completes; `helm status ai-platform-dev -n polaris-dev` shows `deployed` |
| Running `make deploy-delete` again on a namespace that is already Helm-managed makes its live resources disappear (`kubectl -n polaris-dev get deployment,pods,svc,...` reports "No resources found"), while `helm status ai-platform-dev -n polaris-dev` still says `deployed` and shows the same `REVISION` as before *(observed 2026-09-22)* | `scripts/deploy/app.sh delete` (Phase 4) removes objects by name and kind (`kubectl delete deployment/ai-service`, `service/ai-service`, ...) — it has no idea a Helm release now owns objects with those same names. It deletes them out from under Helm without telling Helm, which only stores its release record as a separate Secret object that `app.sh delete` never touches. `deploy-delete` is only meant to run **once**, as the migration step before the very first `make helm-apply-dev` in a namespace (ADR-23) — running it again afterward is the mistake, not a bug in either script | Do not run `make deploy-delete` again once a namespace is Helm-managed. To recover: rebuild/push the image for the current commit if needed (`make image-build && make image-push`), then just `make helm-apply-dev` again — `helm upgrade --install` reconciles the missing objects back into existence using the existing release record, no `deploy-delete` or uninstall needed | `make helm-status-dev` shows all objects again and the `REVISION` number in `helm status` has incremented; `make helm-smoke-dev` passes |
| `make helm-apply-dev`: `Error: INSTALLATION FAILED: ... already exists and is not managed by Helm` *(expected — did not occur 2026-09-22, because `make deploy-delete` was run first)* | The `polaris-dev` namespace still has Phase 4's raw-YAML-managed `ai-service` resources (Deployment/Service/Ingress/PDB/NetworkPolicy) | `make deploy-delete` first (removes those resources, keeps the namespace — ADR-23), then `make helm-apply-dev` | `make helm-apply-dev` completes; `helm status ai-platform-dev -n polaris-dev` shows `deployed` |
| `make helm-lint`: template render fails on a `required` error naming `image.tag` *(expected — did not occur; `make helm-lint` passed cleanly for dev/staging/prod on 2026-09-22)* | `scripts/deploy/helm.sh lint` always passes a placeholder tag — this only happens if the placeholder itself was removed or the chart's `required` guard changed | Compare `helm/ai-platform/templates/deployment.yaml`'s `required` line against `scripts/deploy/helm.sh`'s `LINT_TAG` | `make helm-lint` passes for dev/staging/prod |
| `make helm-apply-<env>`: `Tag <version>-<sha> is not in the local registry` *(observed 2026-09-22)* | Same as Phase 4's equivalent error: the image for the current commit (the Phase 5 chart commit) was never built or pushed before the first `make helm-apply-*` was tried | `make image-build && make image-push` | `curl -s localhost:5000/v2/polaris/ai-service/tags/list` lists the tag |
| `make helm-apply-<env>`: rollout times out; `helm upgrade --install` rolls back automatically (`--wait --timeout 120s`) | Same underlying causes as Phase 4 (PSA rejection, `ImagePullBackOff`, failing readiness) — Helm's `--wait` just surfaces it as a failed install/upgrade instead of a script timeout | `kubectl -n polaris-<env> describe pod <name>`; `helm status ai-platform-<env> -n polaris-<env>` | `make helm-status-<env>` shows `2/2` (or `3/3` for prod) `Ready` |
| Two environments' pods answer each other's `/v1/chat` through the same ingress port | The `Host` header used in `curl`/`make helm-smoke-<env>` does not match that environment's `values-<env>.yaml` `ingress.host` | Use `scripts/deploy/helm.sh smoke <env>`, which reads the host from the right values file instead of hardcoding it | `make helm-smoke-dev` and `make helm-smoke-staging` answer from their own release's pods (different `request_id` prefixes in `make helm-logs-<env>`) |
| `make helm-uninstall-dev` also removes the `polaris-dev` namespace | The Namespace template's `"helm.sh/resource-policy": keep` annotation was removed or a hand-run `kubectl delete namespace` was used instead of the uninstall command | Compare `helm/ai-platform/templates/namespace.yaml` against ADR-23; namespaces are recreated with `make helm-apply-<env>` (`--create-namespace`) if this does happen | `kubectl get namespace polaris-dev` still exists after `make helm-uninstall-dev` |
| `helm upgrade --install` succeeds but the old NetworkPolicy's `describe` still shows the previous release's values | A prior raw `kubectl apply -f networkpolicy.yaml` (Phase 4) is still present alongside the chart's copy of the same object names | `kubectl -n polaris-<env> get networkpolicy` — if both a Phase-4-applied and a Helm-managed copy exist under the same name, the most recent `apply` wins; run `make deploy-delete` before the first `make helm-apply-<env>` in a namespace to avoid this entirely | `kubectl -n polaris-<env> get networkpolicy ai-service-allow-ingress -o jsonpath='{.metadata.labels.helm\.sh/chart}'` is set |

## Phase 6: CI pipeline (`.github/workflows/ci.yml`)

Rows below split three ways. *(observed 2026-09-22, sandbox)* means it genuinely ran in the sandbox and this is the real result — the Docker-free steps (shell lint, static tests, secret scan, dependency audit, workflow lint) could actually execute there, unlike every previous phase's scripts. *(observed 2026-09-22, GitHub Actions)* means it happened on a real GitHub-hosted runner, on this repository's first-ever full pipeline run. *(expected)* means written from how these tools usually fail, not reproduced anywhere.

**The full pipeline has now run for real and finished green, end to end**, including the two jobs that need Docker and a cluster — `build-scan-deploy` (image build, hardened-container check, Trivy scan, SBOM, ephemeral k3d cluster, Helm deploy, smoke test) and `publish` (GHCR push) — for the first time in this project's history. It took three attempts on the same underlying push before that first job even started, because the first two real GitHub Actions runs surfaced two genuine bugs that no amount of local testing had ever caught (see the two rows immediately below): a file that had never actually been committed, and a test regression introduced while fixing that. Both are now fixed and confirmed by a subsequent green run (commit `48cb3a1`, run #4: all six jobs passed, `build-scan-deploy` succeeded in 2m 1s, `publish` succeeded). This is the clearest evidence so far in this project that local, single-machine verification and a genuinely independent checkout are not the same thing — see ADR-24's update.

| Symptom | Cause | Fix | Confirm |
|---------|-------|-----|---------|
| `.github/workflows/ci.yml`, `lint-and-test` job's shellcheck step: `scripts/build/*.sh: scripts/build/*.sh: openBinaryFile: does not exist (No such file or directory)`, `make: *** [Makefile:30: lint] Error 2` *(observed 2026-09-22, GitHub Actions — run #1, commit `f8512c7`, this project's very first real Actions run)* | `.gitignore`'s `build/` entry (no leading slash) is an **unanchored** pattern — git treats a directory pattern with no other slash as matching at any depth, equivalent to `**/build/`. It was written to catch a top-level Python build-artifact directory, but it also silently matched `scripts/build/` — a real, permanent source directory holding `scripts/build/image.sh`, the script every phase since Phase 3 has used to build/check/push/scan/SBOM the image. `git log --oneline --all -- scripts/build/image.sh` produced zero output: the file had never once been committed, in any commit, on any branch, since it was first written in Phase 3 — only ever present as an untracked file on local disk. Every previous phase's local verification (and this phase's own first local run) worked anyway, because scripts read straight from disk regardless of git tracking status. Only a genuinely independent checkout — which this project never had until GitHub Actions cloned it fresh — could expose this | Anchor the pattern to the repository root with a leading slash (`/build/` instead of `build/`); the same latent risk existed for `dist/` and `artifacts/`/`.venv-ci/`, anchored the same way even though nothing currently collides with them. Then stage `scripts/build/image.sh` for the first time ever — `git add scripts/build/image.sh` reports it as a brand-new file (`A`, `create mode 100755`), not a modification | `git check-ignore -v scripts/build/image.sh` prints nothing (no longer ignored); `git log --oneline -- scripts/build/image.sh` shows the commit that finally added it; the `lint-and-test` job's shellcheck step passes |
| `tests/bootstrap/test_static.sh` (via `make test`), check #35: `[FAIL] .gitignore must exclude .venv-ci/`, `PASSED=126 FAILED=1` *(observed 2026-09-22, GitHub Actions — run #3, commit `7d30b5c`, immediately after the fix above)* | A regression introduced by fixing the row above, not a pre-existing bug. The check used an exact whole-line match, `grep -qxF '.venv-ci/' .gitignore`. Anchoring `.gitignore`'s `.venv-ci/` entry to `/.venv-ci/` changed the literal text of that line, so the exact match no longer found it. `.venv-ci/` itself was never actually untracked by this — it was still correctly gitignored — only the test's string comparison was too strict to recognize the new, equally correct anchored form | Relax the check to accept either form: `grep -qxE '/?\.venv-ci/' .gitignore` instead of `grep -qxF '.venv-ci/' .gitignore` | `make test` reports `PASSED=127 FAILED=0` including `[ ok ] .venv-ci/ is gitignored`; the `lint-and-test` job's "Static tests" step passes |

| Symptom | Cause | Fix | Confirm |
|---------|-------|-----|---------|
| `.github/workflows/ci.yml`, the image-save step: shellcheck (via actionlint) flags `SC2046: Quote this to prevent word splitting` on `docker save --output ... $(docker images ...)` *(observed 2026-09-22, sandbox — caught by `make ci-workflow-lint` before this file was ever pushed)* | An unquoted `$(...)` command substitution is correct here in spirit (it is meant to expand to several image references as separate words) but shellcheck cannot tell that apart from an accidental unquoted substitution that should not split, and warns either way | Use a bash array instead of relying on word splitting: `mapfile -t images < <(docker images --filter '...' --format '{{.Repository}}:{{.Tag}}')` then `docker save --output ... "${images[@]}"` | `make ci-workflow-lint` reports "actionlint: no issues" |
| `make ci-secrets-scan`: `gitleaks not found` | `gitleaks` was never installed, or was installed to a `--prefix` that is not on `PATH` | `make tools-install` (installs all five pinned tools) or `make tools-install ARGS="--only gitleaks"` for just this one; make sure `~/.local/bin` is on `PATH` (the same note `make tools-install` already prints for k3d/kubectl/helm) | `gitleaks version` prints `8.30.0` |
| `make ci-secrets-scan`: `[warn] no .git directory here; scanning the working tree only` *(observed 2026-09-22, sandbox — expected there by design, not a bug)* | The sandbox this project is developed in is not itself a git checkout (see the Phase 4/5 entries above for the same root cause) | Nothing to fix — this is `scripts/ci/secrets-scan.sh`'s documented fallback (`--no-git`). On a real checkout (the target machine, or a GitHub Actions runner with `fetch-depth: 0`, as the workflow requests) gitleaks scans the full committed history instead, so a secret added and later removed is still caught there | `gitleaks detect --source . -v` (without `--no-git`) runs against history on a real checkout |
| `make ci-secrets-scan` fails with `gitleaks found N potential leak(s)` *(expected)* | Something that looks like a credential (an API key pattern, a private key header, a high-entropy string) is present in a tracked file | Read `artifacts/gitleaks-report.json` (values are redacted; file/line/rule are not) to find exactly what and where. If it is a real secret: rotate it immediately, then remove it from history (not just the latest commit) before pushing — `git filter-repo` or the BFG Repo-Cleaner, not `git rm` alone. If it is a false positive (a test fixture, an example key that was never real): add a `.gitleaksignore` entry for that exact finding, do not disable the rule globally | `make ci-secrets-scan` reports "no leaks found" |
| `make ci-deps-audit` fails, listing a CVE against a package in `app/ai_service/requirements.txt` *(expected)* | A pinned runtime dependency has a publicly known vulnerability | Check whether a newer pinned version fixes it (`pip index versions <package>` or the advisory itself), bump the exact pin in `requirements.txt`, re-resolve `requirements-dev.txt` if it also pins that package, then `make app-check` before re-auditing | `make ci-deps-audit` reports "no known vulnerabilities" |
| `make ci-deps-audit`: `.venv-ci/bin/pip-audit: No such file or directory` right after interrupting a first run | The throwaway venv was created but the `pip install` step never finished (Ctrl-C, network drop) | `rm -rf .venv-ci` and run again — the script only treats the venv as ready once `pip-audit` is actually installed in it, so a half-made one is not silently reused, but a stale *empty* directory from an interrupted `python3 -m venv` can still confuse the `[[ -x ... ]]` check | `make ci-deps-audit` recreates `.venv-ci` and completes |
| `make ci-workflow-lint`: `actionlint not found` | Same cause/fix as the gitleaks row above, for `actionlint` instead | `make tools-install` or `make tools-install ARGS="--only actionlint"` | `actionlint -version` prints `1.7.12` |
| `make ci-workflow-lint`: yamllint fails on `line-length` or `document-start` against a hand-edited workflow file *(expected)* | `.yamllint.yml` at the repo root was not picked up (wrong working directory), or a new workflow file was added without checking it against the existing relaxed rules | Run `make ci-workflow-lint` from the repo root (it already does, via `repo_root()`); if a genuinely long line is unavoidable (a long `uses:` ref with a pinned SHA, for example), that is exactly what `line-length: max: 140` in `.yamllint.yml` already allows — check the actual column count before assuming the config is wrong | `make ci-workflow-lint` reports "yamllint: no issues" |
| `make image-build`/`make image-scan`/ephemeral `make cluster-up` fail in this sandbox with `Cannot connect to the Docker daemon` *(observed 2026-09-22, sandbox — expected there by design)* | The sandbox's `docker` CLI is present but no daemon is reachable (`docker info` fails) — unlike Phase 5's Helm gap, this is not a missing binary, it is the whole class of "run a container" being unavailable here | Nothing to fix in the sandbox. These steps run for real for the first time on the target machine (`make image-build`, `make image-scan`, etc., exactly as Phase 3/4/5 already did) and, separately, the first time `.github/workflows/ci.yml` executes on a GitHub-hosted runner after a push | `docker info` succeeds on the target machine and on a GitHub Actions runner (both have a real Docker daemon) |
| `.github/workflows/ci.yml`, `build-scan-deploy` job: `make cluster-up` never sees Traefik appear, same symptom as the Phase 1 entry above *(expected — same root cause as Phase 1's Traefik row, GitHub-hosted runners pull from Docker Hub too)* | A fresh k3d cluster on the runner pulls its own system images from Docker Hub, the same as a fresh cluster anywhere; `deploy/k3d/cluster.yaml`'s wait logic and `POLARIS_TRAEFIK_WAIT` apply identically on a runner | Nothing workflow-specific to fix; if it recurs often on GitHub-hosted runners specifically, raise the wait in the workflow step (`POLARIS_TRAEFIK_WAIT=900`) the same way the Phase 1 guide already documents for a slow local pull | The `build-scan-deploy` job's "Create the ephemeral k3d cluster" step succeeds within the default 600 s |
| `.github/workflows/ci.yml`, `publish` job: `docker push` fails with `unauthorized` or `denied` *(expected)* | `docker login ghcr.io` either did not run before `make image-publish`, or `GITHUB_TOKEN` lacks `packages: write` for this run (a fork's pull request never gets `publish` at all — it only runs on a push to `main`, by design) | Check the job's `permissions:` block (`packages: write` must be present on the `publish` job specifically, not just inherited); confirm the `docker login ghcr.io` step ran and succeeded before the `make image-publish` step | `make image-publish` reports "published: ghcr.io/.../ai-service:<tag>" |
| `scripts/build/image.sh publish`: `Could not determine the GitHub owner/repo` *(expected)* | Run outside GitHub Actions (no `$GITHUB_REPOSITORY`) and outside a git checkout with a GitHub `origin` remote, or `origin` points somewhere other than GitHub | Set `GITHUB_REPOSITORY=owner/repo` explicitly, or run from a normal clone of this repository (`git remote -v` should show a `github.com` URL) | `./scripts/build/image.sh info` runs without error first (confirms git/versions.env are readable), then `make image-publish` resolves the owner/repo |

## Phase 7: post-deploy verification (`scripts/verify/post-deploy.sh`)

Written and shellchecked in the sandbox, where there is no cluster to run it against (the same gap every cluster-dependent script has had before its first target-machine run). Rows marked *(expected)* are written from how `kubectl`/`curl`/`jq` usually fail here, not yet reproduced. The guide's Testing section asks for one deliberately-triggered failure (`--set config.POLARIS_MOCK_READY=false`) specifically so this table's first real entries are a genuine failure the script caught, not just a happy path — replace the rows below with the actual output the first time each one happens, the same rule every earlier phase followed.

| Symptom | Likely cause | Fix | Confirm |
|---------|--------------|-----|---------|
| `make verify-dev`: `Cannot reach cluster context 'k3d-polaris'` *(expected)* | The cluster is not running, or was created under a different name | `make cluster-up`; `kubectl config get-contexts` | `kubectl --context k3d-polaris get nodes` shows nodes |
| `make verify-dev` FAILs on `rollout` with a timeout *(expected)* | The Deployment has not finished rolling out — not enough replicas Ready within `VERIFY_ROLLOUT_TIMEOUT` (default 60s), often because `make helm-apply-dev` itself is still in progress or just failed | `make helm-status-dev`; `kubectl -n polaris-dev describe pod <name>` for the real reason (same causes as Phase 4/5's rollout rows: `ImagePullBackOff`, a failing readiness probe, insufficient CPU/memory) | `make verify-dev` reports `PASS rollout: ...` |
| `make verify-dev` FAILs on `healthz`/`readyz` with "no response" *(expected)* | Same causes as Phase 5's `helm-smoke-<env>`: wrong `Host` header, k3d load balancer port mismatch, or the pods are not actually Ready yet | `curl -v -H 'Host: ai-dev.localhost' http://localhost:8088/healthz` (adjust host per `values-<env>.yaml`); `make helm-status-dev` | The relevant check reports `PASS` |
| **Round 1 (bug):** `make verify-dev` kept reporting all six checks **PASS**, with identical pod names/metrics before and after `--set config.POLARIS_MOCK_READY=false`, even though `helm upgrade` reported success (REVISION incremented) *(observed on the target machine, 2026-09-22)*. **Round 2 (fix, confirmed on the target machine, same day):** after adding a `checksum/config` annotation, the same deliberate override produced a genuine `FAIL rollout: Waiting for deployment "ai-service" rollout to finish: 1 out of 2 new replicas have been updated...`, with a real third pod (`ai-service-75858d46dd-m5wz4`, a new ReplicaSet hash) that never became Ready — while `healthz`/`readyz`/`ai-answer` stayed `PASS` throughout, because the two old, healthy pods (`ai-service-6f7c56f7c9-*`) kept serving traffic through Traefik during the stuck rolling update, exactly as flagged in the Phase 7 guide's "read this first" note. After rebuilding and pushing the image for the fix's own commit (`make image-build && make image-push` — needed because `scripts/deploy/helm.sh` ties the image tag to the current git SHA) and `make helm-apply-dev`, `make verify-dev` reported a clean `PASS` again, with a fourth, distinct ReplicaSet hash (`ai-service-56cc8988fd-*`), confirming both the fix and the recovery | `helm/ai-platform/templates/deployment.yaml`'s container used `envFrom: - configMapRef: {name: ai-service-config}` with **no checksum/hash annotation anywhere on the pod template**. Kubernetes only rolls a Deployment when its own `PodTemplateSpec` hash changes — referencing a ConfigMap by name is not part of that hash, so a ConfigMap-*content*-only change (exactly what `--set config.POLARIS_MOCK_READY=false` does) never triggered a new ReplicaSet in round 1. `helm upgrade` genuinely created a new ConfigMap revision, but the already-running pods never restarted and never picked up the new value — there was nothing actually unhealthy left for the script to catch; it behaved correctly given what was really running | Added a `checksum/config` annotation to the pod template (`spec.template.metadata.annotations` in `deployment.yaml`), computed from `configmap.yaml`'s rendered content: `{{ include (print $.Template.BasePath "/configmap.yaml") . \| sha256sum }}` — the standard Helm pattern for this exact problem. The sha256 mechanism was hand-verified in the sandbox first (no working `helm` binary there; `get.helm.sh` is unreachable from it, the same gap `versions.env` already documents for gitleaks/actionlint) by rendering `configmap.yaml`'s `data:` block for both config values and confirming the hashes differ; static check #50 in `tests/bootstrap/test_static.sh` now enforces the annotation is present so this cannot regress silently. Fixed and committed `b3af03d` | Confirmed for real, target machine, 2026-09-22: `make verify-dev` reports `PASS rollout: ...` on a healthy release and a genuine `FAIL rollout: ...` right after the deliberate `POLARIS_MOCK_READY=false` override — both proven, not assumed |
| `make verify-dev` FAILs on `ai-answer` with "response.response was empty" *(expected)* | The mock backend returned a technically-valid envelope with an empty string, or a real backend (Phase 11+) returned an empty completion | `make helm-logs-dev` around the request's `request_id`; for the mock backend specifically, this should not happen — treat it as a real regression in `app/ai_service`, not a flaky check | `make verify-dev` reports `PASS ai-answer: ...` |
| `make verify-dev` FAILs on `logs` with "no log lines from the last 5m" *(expected)* | The pods restarted recently and have not logged anything yet in the default `VERIFY_LOG_WINDOW`, or `kubectl logs` is reaching the wrong label selector | `make helm-logs-dev`; widen the window: `VERIFY_LOG_WINDOW=30m make verify-dev` | `make verify-dev` reports `PASS logs: N/N recent lines are valid JSON` |
| `make verify-dev` reports `WARN metrics: kubectl top pod returned nothing yet` *(expected, and by design not a failure — see ADR-25)* | k3s's bundled `metrics-server` has not reported for these pods yet — it can take roughly a minute after a fresh rollout or cluster start before `kubectl top pod` returns anything | Wait a minute and re-run, or check it directly: `kubectl --context k3d-polaris top pod -n polaris-dev`. This warning never fails the overall verification (check 46 in `tests/bootstrap/test_static.sh` enforces that `check_metrics` can only warn) | `make verify-dev` reports `PASS metrics: ...` once metrics-server has reported at least once |
| `make verify-dev`: `jq not found` *(expected)* | `jq` was never installed on this machine (it is not one of the tools `make tools-install` pins — Phase 3/5's scripts already assumed it was present via the distro) | `sudo apt-get install -y jq` | `jq --version` prints a version |
| `make helm-apply-dev`: `[FAIL] Tag <version>-<sha> is not in the local registry (localhost:5000). Run: make image-build && make image-push` right after committing a chart or script change *(observed on the target machine, 2026-09-22, while applying the checksum/config fix above)* | Not a bug — `scripts/deploy/helm.sh` computes the image tag from the *current git SHA* (same as Phase 3/4), so any commit changes the expected tag, and the image for a brand-new commit has, by definition, never been built or pushed yet | `make image-build && make image-push`, then `make helm-apply-dev` again | `make helm-apply-dev` succeeds; `make verify-dev` reports `PASS` |

## Phase 8: GitOps with Argo CD (`scripts/bootstrap/argocd.sh`, `scripts/gitops/*.sh`, `polaris-gitops`)

Written and shellchecked in the sandbox, where there was no cluster to install Argo CD *into*. Every step (Steg 0–12 of the Phase 8 guide) has since been run for real on the target machine, 2026-09-23: `polaris-gitops` created and pushed, `make argocd-install` fully successful (all 6 Deployments + 1 StatefulSet `Ready`, CRD annotation bug fixed), `make gitops-bootstrap`, `ai-platform-dev`/`-staging` synced automatically and `make verify-dev`/`make verify-staging` both `PASS`, `ai-platform-prod` promoted manually and `make verify-prod` `PASS`, and the self-heal behaviour observed live. Four real bugs were found and fixed along the way (rows below); two situations that looked like bugs turned out to be correct, documented behaviour once actually investigated (also below) — the same "diagnose from real evidence, never guess" rule every earlier phase's table follows.

| Symptom | Likely cause | Fix | Confirm |
|---------|--------------|-----|---------|
| `make test`: `[FAIL] polaris-gitops sibling checkout has invalid YAML:` (empty detail) and `[FAIL] polaris-gitops sibling checkout is missing: bootstrap/root-app.yaml ...`, `PASSED=167  FAILED=2` *(observed on the target machine, 2026-09-23)* | A genuine bug in static checks 62–63, found on their first real run: they gated on the sibling `polaris-gitops` directory merely *existing*, not on it holding real content. A retried `unzip` that fails (wrong path, file not yet downloaded — see the next row) can still leave an empty directory behind from its own `mkdir -p`, and an empty directory is not the same as "nothing there yet" from the check's point of view — it ran, found zero `*.yaml` files, and reported that as a failure instead of skipping. Fixed by gating on `bootstrap/root-app.yaml` specifically (the strongest single signal of a real checkout) instead of the bare directory | Update `tests/bootstrap/test_static.sh` to the version in `polaris-ai-platform-phase8-fix.zip` (or pull the fix directly) | `make test` reports `PASSED=169  FAILED=0` again once `polaris-gitops` is either fully populated or genuinely absent — never a partial/empty directory in between |
| `git push -u origin main` (in a fresh `polaris-gitops` checkout): `ERROR: Repository not found. fatal: Could not read from remote repository.` *(observed on the target machine, 2026-09-23)* | The empty `arash00009/polaris-gitops` repository was never actually created on GitHub before the local push — `git remote add origin ...` succeeds regardless (it only records a URL), so the failure only surfaces at `push` | Create the empty repository on GitHub first (Phase 8 guide, Steg 1) — no README/license/`.gitignore` initialised there, since the content already exists locally — then `git push -u origin main` again | `git push -u origin main` succeeds; the repository's file list on GitHub matches the local checkout |
| A `make` command run from inside `~/polaris/polaris-gitops` fails with `make: *** No rule to make target '<target>'.  Stop.` *(observed on the target machine, 2026-09-23, as a direct consequence of the row above)* | `polaris-gitops` has no `Makefile` at all — it is a pure configuration repository (ADR-08), not a copy of `polaris-ai-platform`. This happens after a `cd ~/polaris/polaris-gitops && git push ... && cd -` chain: when the `push` fails (the row above), `&&` short-circuits and `cd -` never runs, silently leaving the shell in the wrong repository for every command after it (`make argocd-install`, `make gitops-bump-*`, ...) — including a `kubectl ... patch application ...` that then fails with `the server doesn't have a resource type "application"`, because `make argocd-install` never actually ran either | `cd ~/polaris/polaris-ai-platform` explicitly before any `make` command — do not rely on a `cd ... && ... && cd -` chain surviving a failure in the middle. Run the fixed GitHub-repo row above first, `pwd` to confirm you are back in `polaris-ai-platform`, then continue from Steg 5 | Every `make argocd-*`/`make gitops-*` command is run with `pwd` reporting `~/polaris/polaris-ai-platform` |
| `make argocd-install`: `The CustomResourceDefinition "applicationsets.argoproj.io" is invalid: metadata.annotations: Too long: may not be more than 262144 bytes`, on both a first attempt and a retry *(observed on the target machine, 2026-09-23)* | A real, well-known `kubectl` limitation, not a transient fluke: plain `kubectl apply -f` (client-side apply) writes the entire previous object into a `kubectl.kubernetes.io/last-applied-configuration` annotation, and the `applicationsets.argoproj.io` CRD's `OpenAPIV3Schema` (one entry per ApplicationSet generator type) is large enough that this annotation exceeds the Kubernetes API server's 262144-byte (256 KiB) cap on total annotation size. Every other resource in the same manifest still applies fine — only this one CRD is rejected, which is why `argocd-applicationset-controller` never becomes healthy even after everything else looks fine | Fixed in `scripts/bootstrap/argocd.sh`: `kubectl apply` now runs with `--server-side --force-conflicts`, which never writes that annotation. `--force-conflicts` is needed because a cluster that already has most resources applied client-side from a failed earlier attempt would otherwise refuse to reassign field ownership to the server-side-apply manager | `make argocd-install` no longer errors on the `applicationsets.argoproj.io` line, and `kubectl --context k3d-polaris get crd applicationsets.argoproj.io` exists |
| `make argocd-status` (checked repeatedly over several minutes after `make argocd-install`): every control-plane pod stays `ContainerCreating` or `Init:0/1`, `READY 0/1`, with no progress *(observed and diagnosed on the target machine, 2026-09-23, via `kubectl describe pod`/`get events`)* | Not a stuck cluster — a slow one, self-resolving. `kubectl describe pod`'s Events showed the `quay.io/argoproj/argocd:v3.5.2` image (≈211 MB) took 11–13 minutes to pull on its *first* pull per node (`Successfully pulled image ... in 13m18.415s`), evidently a slow/throttled path to `quay.io` from this network — every later pull of the same already-cached image on the same node took 1–7 seconds. Because `argocd-redis`'s own `secret-init` init container (which generates the `argocd-redis` Secret holding `REDIS_PASSWORD`) was itself waiting on that same slow first pull, `argocd-server`/`argocd-application-controller`/`argocd-repo-server` each hit one transient `Error: secret "argocd-redis" not found` while starting — kubelet's normal retry-on-failure resolved all three on its own once the Secret existed, with no manual action taken. By the time `describe`/`get events` was run (~13 minutes after install), `polaris-root`, `ai-platform-dev` and `ai-platform-staging` had already reconciled to `Synced`/`Healthy` on their own | Nothing to fix — wait it out. If a first `make argocd-install` on a fresh cluster takes 10–15 minutes to settle on a home internet connection, that is expected, not a failure; `ARGOCD_ROLLOUT_TIMEOUT=900s make argocd-install` avoids the script's own rollout-wait giving up too early on a slow link (the raw `kubectl apply` step itself has no timeout, only the wait loop does) | `make argocd-status` shows all control-plane pods `Running`/`1/1`, and `kubectl -n argocd get events --sort-by=.lastTimestamp \| tail -20` shows no repeating `Warning`/`BackOff` lines other than `argocd-applicationset-controller`'s (a *different*, already-diagnosed cause — the row above) |
| `make gitops-bump-<env> ARGS=--push`, run a second time with nothing actually changed (e.g. the same promotion command pasted twice in a row): `[FAIL] git commit/push failed in .../polaris-gitops — is it a clean checkout with a remote configured?`, even though the checkout is fine *(observed on the target machine, 2026-09-23)* | A real bug in the old `scripts/gitops/bump-image-tag.sh`: `git commit` exits non-zero when there is nothing staged (the file already had that exact tag), and the old `A && B || die` chain treated that identically to a genuine commit/push failure, with a misleading message | Fixed: the script now checks `git diff --cached --quiet` right after `git add`, and when there is nothing to commit it logs `<env> is already at <tag> — nothing to commit or push` instead of failing. A real `git commit` failure and a real `git push` failure (for example the "Repository not found" row above) now also get distinct, separate messages | Running the same `make gitops-bump-<env> ARGS=--push` command twice in a row succeeds both times — the second run is a harmless no-op |
| `make argocd-install`: `Checksum mismatch for the Argo CD v3.5.2 install manifest` *(expected)* | The manifest changed since `ARGOCD_INSTALL_SHA256` was pinned (a re-tagged release upstream — rare but not impossible — or a corrupted/interrupted download, or a MITM'd mirror) | Re-download by hand and inspect it before trusting it: `curl -fsSL -o /tmp/argocd-install.yaml https://raw.githubusercontent.com/argoproj/argo-cd/v3.5.2/manifests/install.yaml && sha256sum /tmp/argocd-install.yaml`. If the new hash is legitimately different (confirmed against the release notes / a second network path), update `ARGOCD_INSTALL_SHA256` in `versions.env` deliberately, the same procedure as any other pin bump in that file. Never just delete the check | `make argocd-install` reports "Checksum verified" and proceeds |
| `make argocd-install`: a Deployment or the StatefulSet never reaches `rollout status ... successfully rolled out` before `ARGOCD_ROLLOUT_TIMEOUT` (default 180s) *(expected)* | Same general causes as any first-time rollout on a fresh k3d cluster (Phase 1/4's rows): system images (`quay.io/argoproj/argocd`, `redis`) still pulling on a slow connection, or insufficient CPU/memory on a small WSL2 VM running Argo CD alongside three `ai-platform` releases already | `kubectl --context k3d-polaris -n argocd describe deployment/<name>` (or `describe statefulset/argocd-application-controller`) for the real reason; `ARGOCD_ROLLOUT_TIMEOUT=420s make argocd-install` on a slow first pull | `make argocd-status` shows all control-plane pods `Running`/`Ready` |
| `make gitops-bootstrap`: `Root Application not found: .../bootstrap/root-app.yaml` *(expected)* | `polaris-gitops` has not been cloned next to `polaris-ai-platform` yet (the default `GITOPS_DIR`), or it was cloned somewhere else without passing that path | `git clone <polaris-gitops URL> ~/polaris/polaris-gitops` (a sibling of this repository), or `make gitops-bootstrap GITOPS_DIR=/path/to/polaris-gitops` | `make gitops-bootstrap` applies the root Application |
| `make argocd-status`: an `Application`'s `SYNC` column shows `Unknown` or its `HEALTH` column shows `Missing` right after `make gitops-bootstrap` *(expected)* | Argo CD has not completed its first reconciliation yet (repository clone, chart render) — this is normal for the first ~10-30 seconds after an `Application` is created, not a failure | Wait and re-run `make argocd-status`; if it persists, `kubectl --context k3d-polaris -n argocd describe application ai-platform-dev` for the real error (a bad `repoURL`, an unreachable Git host, a chart render error) | `SYNC` becomes `Synced` (dev/staging) or `OutOfSync` with a clear reason (prod, before its first manual sync); `HEALTH` becomes `Healthy` |
| An `Application` stays `Degraded` or `OutOfSync` after a sync that should have succeeded *(expected)* | Most likely the deliberate namespace-recreate migration step (`make helm-uninstall-<env>` + `kubectl delete namespace polaris-<env>`) was skipped, and the namespace still has resources from the old, Helm-CLI-managed release that Argo CD does not recognize as its own, so it cannot safely reconcile them | Confirm the migration actually ran: `kubectl --context k3d-polaris get namespace polaris-<env>` should not exist right before the first `gitops-bootstrap`-driven sync for that environment (it is recreated by the chart's own `templates/namespace.yaml` on the next successful sync, `helm.sh/resource-policy: keep`, same as every Helm-CLI deploy before it, ADR-23) | `kubectl -n argocd get application ai-platform-<env> -o jsonpath='{.status.health.status}'` reports `Healthy` |
| `make verify-dev` FAILs with the same class of error Phase 7 already documents above, after switching an environment over to Argo CD management *(expected)* | Argo CD synced an `image.tag` that is not actually in the local registry — either `environments/<env>/image.yaml` still has its placeholder empty tag, or `scripts/gitops/bump-image-tag.sh` wrote a tag before `make image-build && make image-push` had actually pushed it, or `prod` was synced (manually) before either of those | `make gitops-bump-<env>` (which refuses to run unless the tag is already in the registry — this class of error should be caught *before* it ever reaches `environments/<env>/image.yaml`, not after); `kubectl -n polaris-<env> describe pod <name>` for `ErrImagePull`/`ImagePullBackOff` if it did reach the cluster anyway | `make verify-dev` reports `PASS` |
| `apps/prod-app.yaml`'s Application stays `OutOfSync` no matter how long you wait *(expected, and correct — not a bug)* | `prod` deliberately has no `syncPolicy.automated` (ADR-26): `OutOfSync` after a merge to `polaris-gitops` main is the manual-approval gate working as designed, not a failure to reconcile | Trigger the sync explicitly once you actually want to promote: `kubectl --context k3d-polaris -n argocd patch application ai-platform-prod --type merge -p '{"operation":{"sync":{"revision":"HEAD","prune":true}}}'` (see `apps/prod-app.yaml`'s own header comment in `polaris-gitops`) | `SYNC` becomes `Synced`; `make verify-prod` reports `PASS` |
| A manually-run `kubectl scale`/`kubectl edit` against a `dev` or `staging` resource reverts within roughly one Argo CD reconciliation interval, without anyone re-applying anything *(observed and confirmed on the target machine, 2026-09-23 — the intended self-heal demonstration, not a bug)* | `syncPolicy.automated.selfHeal: true` on `apps/dev-app.yaml`/`apps/staging-app.yaml` — Argo CD treats the live cluster state as wrong whenever it diverges from Git, and corrects it back, exactly as `docs/architecture.md`'s GitOps principles table calls for ("Manual `kubectl edit` is reverted by Argo CD self-heal — demonstrated in Phase 8/19"). Confirmed live: `kubectl -n polaris-dev scale deployment ai-service --replicas=1` was immediately followed, unprompted, by Argo CD scaling it back — `kubectl get deployment -w` showed `1/1` → `1/2` → `2/2` within the same reconcile cycle, with no `kubectl apply`/`helm upgrade` run in between | Nothing to fix — this is the check the Phase 8 guide's self-heal step asks you to perform and observe, not an error state | `kubectl -n polaris-dev get deployment ai-service -o jsonpath='{.spec.replicas}'` returns to the value in `values-dev.yaml` on its own |
| `make gitops-bump-<env> ARGS=--push`: `[FAIL] Tag <version>-<sha>-dirty is not in the local registry` right after unzipping a fix onto the repo *(observed on the target machine, 2026-09-23 — expected, not a bug)* | `scripts/gitops/bump-image-tag.sh` computes the tag the same way Phase 3/4/5 always have: `git rev-parse --short=12 HEAD`, plus a `-dirty` suffix whenever `git status --porcelain` is non-empty. Unzipping a fix (`polaris-ai-platform-phase8-fix3.zip`, say) overwrites already-tracked files without committing them, so the tree is dirty and the tag reflects that honestly — this is the same traceability guarantee every earlier phase's tagging relies on, not a new bug: an image is never silently deployed under a tag that doesn't actually correspond to a committed, reviewable state | Commit and push the fix first (`git add scripts/... docs/... && git commit -m "..." && git push`), *then* `make image-build && make image-push` for a clean tag, *then* `make gitops-bump-<env> ARGS=--push`. Building and pushing the `-dirty` tag as-is also works mechanically, but defeats the point of the tag for anything meant to represent a real, reviewable deploy | `make gitops-bump-<env> ARGS=--push` succeeds with a tag that has no `-dirty` suffix |
| `make verify-<env>` immediately after a `kubectl patch application ... sync` (no `syncPolicy.automated`, e.g. `prod`): `Error from server (NotFound): deployments.apps "ai-service" not found`, `curl: (22) ... 404` *(expected, if checked too soon)* | The `patch` only schedules the sync operation — Argo CD's application controller still needs to clone the repo, render the chart, and apply the resources before anything exists to verify. A `make argocd-status`/`make verify-<env>` run in the same breath as the `patch` command can easily win the race | Wait a short while (seconds to roughly a minute), then `make argocd-status` again; if `SYNC` is still `OutOfSync` after that, `kubectl --context k3d-polaris -n argocd get application ai-platform-<env> -o jsonpath='{.status.operationState.phase}{"\n"}{.status.operationState.message}'` shows the sync operation's real outcome (`Succeeded`/`Failed`/`Error` and why) instead of guessing | `make argocd-status` shows `Synced`/`Healthy`; `make verify-<env>` then reports `PASS` |
| `make argocd-password`: `argocd-initial-admin-secret not found` *(expected, if the admin password was ever changed)* | Argo CD deletes this Secret the first time the initial admin password is rotated — this project never rotates it (no `argocd` CLI is installed, ADR-26, and the web UI is optional/exploratory only), so seeing this means either Argo CD is not installed yet, or the password was changed by hand through the UI at some point | `make argocd-install` if the `argocd` namespace does not exist at all; otherwise, there is no way back to the initial password — reset it following Argo CD's own upstream documentation, or `make argocd-uninstall && make argocd-install` for a clean local re-install (acceptable here since this is a disposable local environment, not for a real one) | `make argocd-password` prints `admin / <password>` |
| **Phase 8 closed out, end to end, target machine, 2026-09-23:** the `-dirty`-tag row above was followed exactly — `git add`/`commit` (`09ba536`)/`push` of the fix3 files, then `make image-build` (clean tag `0.3.0-09ba536a9584`, no `-dirty`), `make image-push`, then `make gitops-bump-prod ARGS=--push` (`polaris-gitops` commit `21967b7`, pushed) | N/A — this row records the confirmed outcome, not a symptom | N/A | `make argocd-status` immediately showed all four Applications (`polaris-root`, `ai-platform-dev`, `ai-platform-staging`, `ai-platform-prod`) `Synced`/`Healthy`; the `kubectl patch application ai-platform-prod ... sync` command was re-run and, after the short wait the row above describes, `make verify-prod` reported `[ok] POST-DEPLOY VERIFICATION PASSED (prod)` on all six checks; `kubectl -n argocd get application ai-platform-prod -o jsonpath='{.status.operationState.phase}{"\n"}{.status.operationState.message}'` returned `Succeeded` / `successfully synced (all tasks run)` |

## Phase 9: Observability (`app/ai_service/src/ai_service/telemetry.py`, `deploy/platform/observability/`, `scripts/bootstrap/observability.sh`)

The application-layer rows below were reproduced for real in the sandbox, where `pytest` runs against real code (same as Phase 2) — marked *(seen)*. The platform-layer rows (Helm charts, the four observability releases, the cluster itself) cannot be exercised in the sandbox — no cluster, and `get.helm.sh`/`*.github.io` are unreachable here (the same gap ADR-23/ADR-26 already document) — so those are marked *(expected)*, to be confirmed or corrected once `make obs-install` and the `ai-platform` Helm upgrade actually run on the target machine.

**First real target-machine run, 2026-09-24.** Two genuine bugs surfaced that the sandbox could not have caught (no installed package to go stale, no real Loki chart to install against), both found from real command output, not guessed, and both fixed below. `kube-prometheus-stack` itself installed cleanly on the third attempt (`STATUS: deployed`, all four of its pods `Running`). `ai-platform-dev`'s rollout, Loki, Tempo and the OTel Collector are still unconfirmed — the run ended with `make obs-uninstall` (a `make obs-grafana` port-forward that had been working then dropped, letting the rest of a pasted command block run to completion), so the next real run starts from a clean `observability` namespace, not a partial one.

**Second real target-machine run, same day, after the fix above was applied.** The hardcoded-version test fix and the Loki `deploymentMode` fix both worked as intended, confirmed for real: `make test` passed clean (141 passed), and `make obs-install` got `kube-prometheus-stack`, Loki *and* Tempo all the way to `STATUS: deployed` — the run had never reached Loki or Tempo before this fix. It then hit a fourth, new real bug at the OTel Collector step, below. Also fixed below, but — same discipline as always — not yet reconfirmed by an actual `helm install` on the target machine.

| Symptom | Likely cause | Fix | Confirm |
|---------|--------------|-----|---------|
| `make app-check`: `FAILED tests/test_chat_api.py::test_reported_version_matches_the_installed_package — AssertionError: assert '0.4.0' == '0.3.0'` *(observed on the target machine, 2026-09-24)* | A real bug in the test itself, not in `ai_service`: it hardcoded the string `"0.3.0"` as the expected version. `ai_service.__version__` already reads the version from installed package metadata (`ai_service/__init__.py`, `importlib.metadata.version(...)`), which correctly followed `pyproject.toml`'s bump to `0.4.0` this phase — the hardcoded assertion just never got updated to match, and was always going to break on the next version bump regardless of which phase caused it | The test now reads the expected version from `pyproject.toml` itself (`tomllib`) instead of a hardcoded string, so it can never drift out of sync with a version bump again | `make app-check` passes with `141 passed` again; bumping `pyproject.toml`'s version for a future phase no longer requires also hunting down a hardcoded copy of the old one |
| `make obs-install`: `Error: execution error at (loki/templates/validate.yaml:35:4): You have more than zero replicas configured for both the monolithic and simple scalable targets. If this was intentional change the deploymentMode to the transitional 'Monolithic<->SimpleScalable' mode` *(observed on the target machine, 2026-09-24)* | A real chart-usage bug in `loki-values.yaml`: `deploymentMode: SingleBinary` was set (a *deprecated alias* for `Monolithic`, confirmed from the chart's own `values.yaml` comment), but the chart's `validate.yaml` template does not treat the two spellings the same — it only exempts the literal transitional strings `Monolithic<->SimpleScalable`/`SingleBinary<->SimpleScalable`. Worse, `read`/`write`/`backend` (the "simple scalable" targets) default to `replicas: 3` each *regardless of `deploymentMode`* (confirmed by fetching the chart's real `values.yaml`) — setting `deploymentMode` alone, to either spelling, was never going to zero them | `deploymentMode: Monolithic` (the literal, non-deprecated value), plus explicit `read.replicas: 0`, `write.replicas: 0`, `backend.replicas: 0` | **Confirmed on the target machine, 2026-09-24 (second run):** `make obs-install` installed Loki cleanly (`STATUS: deployed`) — the run had never reached this step before the fix |
| `make obs-install`: kube-prometheus-stack's `helm upgrade --install --wait` failed with `context deadline exceeded` at the (then-)default 600s timeout, even though the release's pods went on to become `Running` a few minutes later on their own | The same class of issue Argo CD hit in Phase 8: several images pulling for the first time at once on this network (Prometheus, Grafana, kube-state-metrics, three admission-webhook Jobs) outran the wait timeout, even though Kubernetes' own rollout kept going regardless of Helm's client giving up waiting | `OBS_ROLLOUT_TIMEOUT` default raised from 600s to 900s in `scripts/bootstrap/observability.sh`, matching the number Phase 8's `ARGOCD_ROLLOUT_TIMEOUT` fix already established as right for this exact network | **Confirmed on the target machine, 2026-09-24 (second run):** `make obs-install` completed kube-prometheus-stack without a timeout error |
| `make obs-install`/`make obs-password`: `[FAIL] Cannot reach cluster context 'k3d-polaris'. Create it first: make cluster-up`, immediately after opening a fresh WSL2 terminal, even though the cluster was already up and every `kubectl` command run moments later worked fine *(observed on the target machine, 2026-09-24)* | A cold-start timing hiccup, not an actually unreachable cluster: Docker/the network inside WSL2 had not fully settled in the few seconds between the terminal opening and the very first `kubectl` call this script made. `make obs-status`, run immediately after, succeeded — the cluster was not actually down at any point | `need_kubectl()` now retries the reachability check up to three times, 2s apart, before giving up | If this still fails after the retries, the cluster genuinely is down — `make cluster-status` / `make cluster-up` (not re-triggered on the second run — nothing to confirm either way yet) |
| `make obs-install`: `Error: execution error at (opentelemetry-collector/templates/NOTES.txt:2:3): [ERROR] 'image.repository' must be set. See https://github.com/open-telemetry/opentelemetry-helm-charts/blob/main/charts/opentelemetry-collector/UPGRADING.md for instructions.` *(observed on the target machine, 2026-09-24, second run — the first real run to reach this step at all, since Loki previously blocked it)* | A real breaking change in the `open-telemetry/opentelemetry-collector` chart, confirmed against its own `UPGRADING.md` ("0.172.x to 0.173.0" / upstream issue #1135): as of chart `0.173.x`, the chart no longer defaults `image.repository` to the Contrib distribution — it now fails the install outright unless a repository is set explicitly. `otel-collector-values.yaml` never set one, because it did not need to on the chart version this phase was originally written against | Added `image.repository: "otel/opentelemetry-collector-k8s"` — the chart maintainers' own recommended distribution going forward (same `UPGRADING.md`), cross-checked against its real component manifest (`open-telemetry/opentelemetry-collector-releases`, `distributions/otelcol-k8s/manifest.yaml`) to confirm it actually includes every component this project's config uses (the `otlp` receiver, the `otlphttp` exporter, the `debug` exporter, `batch`/`memory_limiter`, the `healthcheck` extension) | *(expected — not yet reconfirmed)*: `make obs-install` gets the OTel Collector to `STATUS: deployed`; not yet re-run against a real cluster |
| `make obs-status`: `ai-service` `ServiceMonitor` found in `polaris-dev` with a real `AGE`, at a point in the session where `make helm-apply-dev` had already failed outright (client-side "no matches for kind ServiceMonitor" — a failure that happens before any resource in that release is applied, so nothing from that command should have created it) *(observed on the target machine, 2026-09-24, unexplained)* | Not chased down — by the time this was noticed the whole `observability` namespace (and, implicitly, anything that depended on its CRDs) had already been torn down again by the end of the same session, so there was nothing left to inspect. Worth a deliberate check on the next run (`kubectl -n polaris-dev get servicemonitor ai-service -o yaml`, specifically its `creationTimestamp` and any owner references) rather than assuming it is explained by anything above | Not fixed — flagged, not guessed at | Recorded honestly as an open question, not as a diagnosed bug with an invented cause |
| `make obs-grafana`: after several requests were served successfully through the port-forward, `error: lost connection to pod` and `E... "Unhandled Error" ... failed to execute portforward in network namespace ...: failed to connect to localhost:3000 ...: connection refused` *(observed on the target machine, 2026-09-24)* | A known class of flakiness in `kubectl port-forward` against a k3d/WSL2 CNI setup once the forwarded pod's own state changes (here: the same terminal session went on to run `make obs-dashboard-configmap` and `make obs-uninstall` right after, and the uninstall deleted the very pod being forwarded) | Nothing to fix — `make obs-grafana` blocks in the foreground on purpose (Ctrl-C to stop); do not chain more commands after it in the same pasted block unless the intent really is to stop the port-forward and move on, since the next command in the chain only runs once it exits | Re-running `make obs-grafana` on its own reconnects cleanly against a healthy, still-running Grafana pod |
| Symptom | Likely cause | Fix | Confirm |
|---------|--------------|-----|---------|
| A smoke test that attaches the OTel `LoggingHandler` to the root logger, then forces an OTLP export failure (`otel_exporter_otlp_endpoint` pointed at `http://127.0.0.1:1`, a guaranteed connection refusal): the exporter's own failure warning gets logged right back through the same root handler, which tries to export it too *(seen, 2026-09-2x)* | `logging.getLogger().addHandler(otel_handler)` catches every logger in the process, including the OTel SDK's own `opentelemetry.exporter...` logger — so a failed export's warning becomes a new log record the same handler picks up and tries to export again | Attach the handler to the `"ai_service"` logger only, never root (`telemetry.py`'s `setup_telemetry`). Third-party loggers (`httpx`, `uvicorn`, `opentelemetry.*`) never reach it; `"ai_service.access"` still does, because it is a child logger that propagates up | `test_only_the_ai_service_logger_namespace_reaches_the_otel_handler` (`test_telemetry.py`) monkey-patches `handler.emit` and asserts only `ai_service`/`ai_service.access` records arrive at it, never `httpx` |
| That same test initially failed for an unrelated reason: it drove the assertion with `GET /healthz`, which produced no `ai_service.access` record to check at all | `/healthz` is a probe path, logged at `DEBUG` (ADR-20's access-log convention), and `caplog`/the OTel handler were both configured at `INFO` in the test | Switched the test to `POST /v1/chat`, which always produces an `INFO`-level access line | Same test as above passes, and fails again (correctly) if the log level is reverted |
| `TracerProvider.shutdown()` + `LoggerProvider.shutdown()`, timed against the same unreachable `127.0.0.1:1` endpoint, took roughly 8 seconds combined *(seen, real timing measurement)* | The OTel SDK's OTLP exporters default their own export timeout to close to 10 seconds; `shutdown()` waits for the last batch to flush (or time out) before returning, and this project's `terminationGracePeriodSeconds` is 15s total, with 5s of it already spent on the existing `preStopSleepSeconds` (ADR-20) — leaving roughly 10s, not enough margin against an 8s worst case plus everything else in the shutdown path | Added `otel_exporter_timeout_s` (`Settings`, default 3.0, capped at 10) and passed it to both `OTLPSpanExporter(timeout=...)` and `OTLPLogExporter(timeout=...)` | Re-measured: shutdown against the same unreachable endpoint now takes roughly 2.9s per provider; `test_shutdown_is_bounded_even_when_the_collector_is_unreachable` asserts it stays under 8.0s |
| `test_metrics_use_route_templates_and_grouped_status_not_raw_values` initially failed: its own `/healthz`/`/v1/chat`/404 requests were missing from its own `/metrics` scrape *(seen)* | `Instrumentator()` defaults to `prometheus_client`'s process-global `REGISTRY`. Every test in the file calls `create_app()` fresh, so every app instance shared one registry — an earlier test's app kept the middleware that was actually recording observations, and this test's own requests went nowhere it could see | `Instrumentator(registry=CollectorRegistry())` — a dedicated registry per app instance, the same "no shared global mutable state across app instances" rule Phase 2's `ModelBackend` already follows | The same test passes; `/metrics` output for a fresh app shows exactly that app's own requests, with `handler="none"` (not the raw path) for the unmatched-route case |
| `scripts/bootstrap/observability.sh dashboard-configmap`, run for real in the sandbox, produced `'''` (three literal single quotes) in the regenerated ConfigMap's header comment instead of one `'` *(seen)* | The original implementation built the Python script through a double-quoted `python3 -c "..."` string with `\\''`-style escapes meant to produce a literal apostrophe — nested shell-then-Python string escaping, and it escaped wrong | Switched to `DASHBOARD_JSON="$DASHBOARD_JSON" DASHBOARD_CONFIGMAP="$DASHBOARD_CONFIGMAP" python3 - <<'PYEOF' ... PYEOF` — a single-quoted heredoc delimiter, so nothing inside is shell-expanded at all, and the two paths cross via environment variables instead of string interpolation | Re-ran the subcommand and diffed the output against a saved-before copy; the only difference left was the intentionally reworded comment text, not a quoting artifact |
| The `Makefile`'s original `obs-grafana` target referenced `./scripts/bootstrap/cluster.sh name`, a subcommand that does not exist *(caught before ever running, by checking `cluster.sh`'s real subcommands instead of assuming)* | `cluster.sh` only implements `up\|down\|reset\|status` — no existing Makefile target in this project calls `kubectl --context` directly; every cluster-interacting target routes through a wrapper script, and this one had been written without checking that convention first | Added a real `cmd_grafana()` to `observability.sh` (`kctl -n observability port-forward svc/kube-prometheus-stack-grafana 3000:80`); the Makefile target now just calls `./scripts/bootstrap/observability.sh grafana` | `make -n obs-grafana` shows the corrected command; `grep grafana scripts/bootstrap/cluster.sh` confirms that script never defines it |
| The OTel Collector's `ServiceMonitor` would have scraped nothing even with `serviceMonitor.enabled: true`, because the metrics port itself was never exposed *(caught by reading the chart's own `values.yaml` comment, not by running it — no cluster in the sandbox)* | The OTel Collector Helm chart's `ports.metrics` is disabled by default; `serviceMonitor.enabled: true` alone only creates the `ServiceMonitor` resource, it does not open the port that resource points at | Added `ports.metrics.enabled: true` in `deploy/platform/observability/otel-collector-values.yaml`, before `serviceMonitor.enabled: true` | *(expected — confirm on the target machine)*: `kubectl -n observability get servicemonitor otel-collector -o yaml` shows the `metrics` port; Prometheus's Targets page shows it `UP` |
| An initial `values.yaml` comment claimed the dashboard's `30s` refresh/scrape interval "matches kube-prometheus-stack's own default" *(caught by fetching the pinned chart's real `values.yaml`, not assumed)* | `prometheus.prometheusSpec.scrapeInterval` actually defaults to `""` in kube-prometheus-stack 91.5.0, which falls back to Prometheus's own default of about 1 minute, not 30 seconds | Corrected the comment: `30s` is a deliberate, tighter choice for this project, not a claim of matching any upstream default | Read `deploy/platform/observability/kube-prometheus-stack-values.yaml`'s own comment next to `serviceMonitor.interval` |
| `make argocd-install`/`helm upgrade --install ai-platform` fails with `no matches for kind "ServiceMonitor" in version "monitoring.coreos.com/v1"` *(expected — the exact failure mode ADR-27 exists to prevent)* | `ai-platform`'s `ServiceMonitor` template rendered before kube-prometheus-stack's CRDs exist in the cluster — either the observability stack was never installed, or an environment had `serviceMonitor.enabled: true` set before it was | Run `make obs-install` first, confirm the CRD exists (`kubectl get crd servicemonitors.monitoring.coreos.com`), *then* apply/sync any environment with `serviceMonitor.enabled: true`. Base `values.yaml` defaults this to `false` everywhere except `values-dev.yaml` specifically so this only bites an environment that was deliberately, individually opted in | `kubectl get crd servicemonitors.monitoring.coreos.com` exists before `helm upgrade`/Argo CD sync is ever run against an environment with `serviceMonitor.enabled: true` |
| `helm install`/`helm upgrade` for Loki or Tempo silently uses a different chart version than expected, or a values key from an old blog post/tutorial is rejected as unknown *(expected)* | Both charts moved from `grafana/helm-charts` to `grafana-community/helm-charts` during 2026 (Tempo 2026-01-30, Loki 2026-03-16); the old repository's copy is frozen (Loki: Grafana Enterprise Logs only, last OSS release `6.55.0`), and values-file shape can differ meaningfully between the two lineages | Confirm the repo is added correctly (`helm repo list` should show `grafana-community` pointing at `https://grafana-community.github.io/helm-charts`, not the old `grafana` repo for these two charts); `scripts/bootstrap/observability.sh install` adds the right repo and installs without `--version`, printing the resolved version afterward — pin it into `versions.env` (`LOKI_CHART_VERSION`/`TEMPO_CHART_VERSION`) as a deliberate follow-up once that real output exists | `helm list -n observability` shows the actually-installed chart versions; `versions.env` is updated in a follow-up commit, not guessed at now |
